const MainPage = () => {
  return (
    <div>
      <p style={{ fontSize: '40px' }}>
        With the Project Tracker you can effectively manage your projects, share with the teammates,
        create columns and name them according to your requirements, you can change the order of the
        columns if necessary. In each column, you can create tasks, assign performers, move them
        both within the column and between columns.
      </p>
      <p>You can create an unlimited number of projects, columns and tasks.</p>
      <p>Use our app and leave feedback!</p>
    </div>
  );
};

export default MainPage;
